// هذا الملف غير مُفعّل بعد.
// عندما تجهز بيانات مشروع Supabase الخاص بك (Project URL و anon key)
// من لوحة تحكم Supabase → Settings → API، سنملأ القيمتين أدناه معاً
// خطوة بخطوة، ثم نربط نماذج التسجيل والبحث بقاعدة البيانات الفعلية.

const SUPABASE_URL = "REPLACE_WITH_YOUR_SUPABASE_PROJECT_URL";
const SUPABASE_ANON_KEY = "REPLACE_WITH_YOUR_SUPABASE_ANON_KEY";

// لاحقاً سنضيف هنا:
// import { createClient } from 'https://esm.sh/@supabase/supabase-js@2'
// export const supabase = createClient(SUPABASE_URL, SUPABASE_ANON_KEY)
